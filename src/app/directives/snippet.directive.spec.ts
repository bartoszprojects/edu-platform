import { SnippetDirective } from './snippet.directive';

describe('SnippetDirective', () => {
  it('should create an instance', () => {
    const directive = new SnippetDirective();
    expect(directive).toBeTruthy();
  });
});
