import { SnippetTileActiveDirective } from './snippet-tile-active.directive';

describe('SnippetTileActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SnippetTileActiveDirective();
    expect(directive).toBeTruthy();
  });
});
